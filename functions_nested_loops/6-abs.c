#include <stdlib.h>

/**
 * _abs - computes the absolute value of an integer
 * @n: the number to compute the absolute value of
 *
 * Description: Returns the absolute value of the integer passed
 * as an argument.
 *
 * Return: the absolute value of n
 */
int _abs(int n)
{
	return (abs(n));
}
