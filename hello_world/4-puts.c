#include <stdio.h>
/**
 * main - Entry point,
 * print the text \"Programming is like building a multilingual puzzle
 * and add a new line
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle\n");
	return (0);
}
